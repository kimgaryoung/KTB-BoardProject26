name: Self Hosted Runner fronted CD

on:
  workflow_run:
    workflows:
      - Self Hosted Runner frontend CI
    types:
      - completed
  pull_request:
    branches:
      - main



jobs:

  deploy:
    runs-on: self-hosted
    timeout-minutes: 10

    steps:
      - name: Deploy application
        run: |
          echo "배포 시작"
          echo "여기에 docker build / docker run 명령어 작성"
          echo "배포 완료"
